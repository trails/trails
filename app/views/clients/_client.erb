<section>
  <%= render :partial => 'clients/edit', :object => client, :locals => {prefix: prefix} %>
  <span>
    <div class="controls">
      <button class="change"></button>
    </div>
    <h4><%= client.name %></h4>
    <p><%= client.email %>
  </span>
</section>
